dependencies:
  post:
    - wget https://download.elasticsearch.org/elasticsearch/elasticsearch/elasticsearch-1.4.2.tar.gz
    - tar -xvf elasticsearch-1.4.2.tar.gz
    - elasticsearch-1.4.2/bin/elasticsearch: {background: true}
